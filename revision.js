// ? number type variable
var penPrice = 10;
var bananaPrice = 20;

// ? string type variable 
var myName = 'takbir';
var prnColor = 'blue';

// ? boolean type variable
var isExpensive = false;

// ? Array
var tableItems = ['pen', 'monitor', 'pendrive', 'ups', 'speaker', 'mouse'];
var item2 = tableItems[4];
tableItems[5] = 'keyboard';
console.log(tableItems);
console.log(item2);